// The VR Link plugin module. No startup work: everything lives in the
// components and the subsystem, which the engine instantiates itself.

#include "Modules/ModuleManager.h"

IMPLEMENT_MODULE(FDefaultModuleImpl, VrLink)
